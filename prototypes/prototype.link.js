// create a new function for Links
StructureLink.prototype.sendEnergy =
    function () {
        
        
    //     //find other links
    //     var homeLink = structure.pos(27, 11);
        
    //     //If energy inside me is full && not the one in the base
    //         //send it to another link (make sure its the one in the base, by proximity to something? id? hard code pos?)
            
        
        
        
    //     const linkFrom = Game.rooms['W1N1'].lookForAt('structure', 10, 25)[0];
    
    // 	const linkTo = linkFrom.pos.findInRange(FIND_MY_STRUCTURES, 2,
    // 		{filter: {structureType: STRUCTURE_LINK}})[0];
    
    // 	linkFrom.transferEnergy(linkTo);
    }